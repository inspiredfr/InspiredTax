# Owner Interview — Confirmed Decisions (2026-07-05)

These are binding. Do not re-ask or deviate.

1. **Play Store status: NOT yet listed.** No Play Store link exists. Primary CTA = "Notify me at launch" email waitlist + a "Coming soon to Google Play" badge. When the app goes live the owner will swap in the real badge/URL (build the badge slot so this is a one-line change).
2. **FAQ/Guide copy: owner supplies it later.** Build `tax-faq.html`, `app-faq.html`, `tax-guide.html` as fully-styled pages with structured placeholder copy (clearly marked `<!-- OWNER COPY -->`). Nav/footer must link to all three.
3. **Brand: full rebrand** to the new identity — dark navy `#151A2E`-ish background, indigo→cyan blue gradient (per attached logo). Green (`#1FAF73`) is removed entirely.
4. **Cuts:** delete the "App Ecosystem & Strategy Roadmap" section (dashed placeholder boxes); landing page shows only 3 featured articles + link to an `articles/index.html` listing all; remove all "Alpha 1 Testing" wording — use confident pre-launch copy ("Launching soon on Google Play").
5. **Waitlist mechanism: Formspree** free tier. Form posts to `https://formspree.io/f/YOUR_FORM_ID` (owner will supply ID).
6. **Canonical domain: `inspiredtax.co.za`** (matches CNAME). Update every canonical/OG/schema/sitemap URL from inspiredtax.africa → inspiredtax.co.za.
7. **Hero mockup: CSS-built phone mockup** showing a realistic in-app provisional tax summary screen in the new brand style. No image assets needed for it.

## Owner-stated goals (from original brief)
- Premium look; must NOT look AI-generated or boilerplate; no generic/dead links.
- Simple, not unwieldy. Primary funnel → app store (waitlist for now).
- Secondary: tax articles + free calculators.
- Mobile-first; motion & polished "fancy" UX touches.
- Must link to: Tax FAQ, App Use FAQ, Tax Guide.
